export interface IconTemplatePropsInterface {
  /**
   * Optional icon css class
   */
  class?: string;

  /**
   * Whether the icon has a wrapper or not (by convention should be true by default)
   */
  clipped?: boolean;
}
