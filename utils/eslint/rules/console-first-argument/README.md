# Enforce first argument as string in console

The purpose of this lint rule is to enforce the use of a string as first argument in console methods calls.

## Usage:

```js
{
  "pf-rules/console-first-argument-string": "error",
}
```
